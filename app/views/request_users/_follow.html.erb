

<%= form_for(current_volunteer_user.active_relationships.build) do |f| %>
  <div><%= hidden_field_tag :followed_id, @request_user.id %></div>
  <%= f.submit "Follow", class: "btn btn-primary" %>
<% end %>