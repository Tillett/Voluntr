<% @volunteer_user ||= current_user %>
<div class="stats">
  <a href="<%= following_volunteer_user_path(@volunteer_user) %>">
    <strong id="following" class="stat">
      <%= @volunteer_user.following.count %>
    </strong>
    following
  </a>
</div>